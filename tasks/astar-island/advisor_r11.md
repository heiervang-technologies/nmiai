- Use about `1` diagnostic query per seed on average, but allocate by the whole round: **`5` total queries across all 5 seeds**, not `10` per seed. The evidence says gains saturate at `5` viewports and extra queries add no value for regime detection.
- Prefer **`5` different viewports with zero repeats**. Enforce diversity: roughly `3` inland settlement-frontier windows and `2` coastal civ windows with likely port conversion; do not collapse budget into `1x10` or `2x5` repeat sampling.
- Use observations **for regime/template weight updates only**. Do **not** use them for cell-level Bayesian posterior updates before R11; sparse local samples have already shown they can corrupt the calibrated prior.
- Before R11, keep `template_predictor.py` on the regime-only observation path and ensure the submission path actually loads current-round observations. Do not ship any local observation nudging unless it is explicitly re-enabled after validated out-of-sample gains.
- If there is time for one more code change, make it in **query selection**, not posterior math: score viewports by regime-diagnostic value (initial settlements, inland `d≈1-3`, mid-range `d≈3-5`, coastal `n_ocean>=2`) and pick the top `5` with a diversity constraint.
